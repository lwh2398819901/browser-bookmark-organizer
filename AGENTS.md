# 浏览器收藏夹整理工具包

本仓库提供本地优先的收藏夹整理流程。收到任何浏览器收藏夹相关请求时，先阅读 `.agents/skills/bookmark-organizer/SKILL.md`，再决定行动。

## 开发文档查询

- 修改第三方库、浏览器扩展 API、MCP 或其他存在版本差异的接口前，优先使用已配置的 Context7 MCP 查询当前官方文档，再据此设计和实现。
- 不为纯本地业务逻辑、语言标准库、仓库内已有接口或已经由测试明确覆盖的行为机械调用 Context7；避免把文档查询变成每次修改的固定开销。
- Context7 不可用、没有收录目标项目或结果无法回答问题时，改查该项目官方文档，并在仍有不确定性时明确说明。不得把非官方博客当作唯一实现依据。

## 工作边界

- 当前仓库只实现本地使用方案。`docs/future-remote-service.md` 是候选方向，不得因为该文档存在就默认增加联网、账号、云端 Agent 或数据上传能力；只有用户明确要求进入远端实现阶段时才可扩展范围。
- 先选择足以完成任务的最低成本可靠数据源：日常处理临时收藏、备份、查看记录和撤销优先调用技能中的 `scripts/bookmarkctl.py`，由配套扩展返回实时数据；首次全量整理、画像和链接健康检查才运行审计脚本。只对现有信息无法判断的条目打开网页。
- 不要用视觉自动化点击插件页面，也不要为现有 `bookmarkctl` 能力编写一次性脚本。桥接失败时先诊断安装、配置和扩展重载；插件页面只作为人工备用入口。
- 浏览器运行时，绝不直接编辑 Chromium 正在使用的 `Bookmarks` 文件；实时操作只能使用配套的 `收藏夹整理助手（本地）` 扩展，不得将其他相似扩展视作本技能的执行器。
- 保留用户的导出文件和浏览器数据。报告写入用户指定的输出目录，不得提交到 Git。
- “整理收藏夹”通常表示审计与建议；若用户已明确指定条目范围和目标目录，可在预检后移动。整理中认为可以删除的书签主动移入收藏夹栏下的 `待删除`，由用户手工删除。用户表示删完后：文件夹里还有书签就放回合适位置，已经空了就删除 `待删除` 这个文件夹。用户点名要助手直接删除时，书签默认移入回收站；永久删除必须另有明确授权。清空回收站、清理其他空目录、重命名或移动目录，仍需要明确对象或已展示清单上的确认。
- 超时、401、403、429 和登录页均为“无法确认”，不是失效链接。
- `extension/edge-bookmark-organizer` 下名为 `收藏夹整理助手（本地）` 的 Chromium 扩展可通过 `chrome.bookmarks` 应用已复核的移动、软删除、空目录清理和目录重命名/移动计划；先核验其 `manifest.json` 的名称、版本和权限。路径校验应识别浏览器实际的收藏夹栏标题，不得假定所有系统和语言环境都叫“收藏夹栏”。`临时收藏` 与 `回收站` 不参与自动清理。

## 意图理解

不要把表达方式当作固定命令。结合当前对话、用户指向的条目和目标目录理解意图：

| 用户可能的说法 | 通常应进入的工作方式 |
|---|---|
| 整理收藏夹、第一次收拾、这些太乱了 | 全量审计与可复核建议 |
| 整理临时收藏夹、把刚存的归类、处理收件箱 | 收件箱归档、精确去重与移动预检 |
| 整理整个收藏夹、压平目录层级 | 全库实时扫描（`scan --scope all`）、逐条预览、确认后移动；目录删除仍不自动执行 |
| 检查重复、这些是不是存过、相似链接合并 | 精确重复报告；保留不同章节链接 |
| 哪些过期、网站还活着吗、有没有改版 | 链接健康检查与基线比较 |
| 收藏夹画像、我都关注什么、深度分析 | HTML 画像；AI 结论必须有证据 |
| 就按方案放、把这些移到某目录、现在动手 | 重新校验后应用明确范围内的移动 |
| 删完了、待删除我处理过了 | 检查 `待删除`：有剩余则放回合适位置，已空则删除该文件夹 |

不得从收藏内容推断敏感个人特质。始终区分命令生成的事实与 AI 的语义解读。
